<!DOCTYPE html>
<html>
  <head>
    <!-- These lines can (possibly?) be removed if going to turbo -->
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <!-- Uncomment when turbo is installed-- %= stylesheet_link_tag "application", "data-turbo-track": "reload" % -->
    <!-- end -->

    <!-- Not sure why following is no longer shown as needed -->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <title><%= full_title(yield(:title)) %></title>
  </head>

  <body>
    <%= render 'layouts/header' %>
    <div class="container">
      <%# Rails flash messages styled for Twitter Bootstrap 4.0 %>
      <% flash.each do |message_type, message| %>
	      <div class="<%= flash_class(message_type) %>">
          <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                  <span aria-hidden="true">&times;</span></button>
          <%= message %>
        </div>
      <% end %>
      <%= yield %>
      <%= render 'layouts/footer' %>
      <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
</html>
