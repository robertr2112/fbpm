<% flash.each do |message_type, message| %>
  <div class="<%= flash_class(message_type) %>" role="alert">
    <%= message %>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% end %>
